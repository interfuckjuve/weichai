# Domain Docs

Layout: single-context, shared across all workspaces.

Before exploring, read the root `CONTEXT.md` and relevant ADRs
under `docs/adr/`. If absent, proceed silently; create domain
documents only when terms or decisions need recording.

Use the glossary's defined terms in tickets, designs, and tests.
Flag missing concepts for domain modeling.
Surface conflicts with existing ADRs explicitly.
