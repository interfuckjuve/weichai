# 丢弃旧索引并重新初始化

用户已明确不需要旧分析数据。当前恢复方案是在原数据库名下删除并重新初始化代码智能数据库，不再使用保留数据迁移、触发器或临时禁止写入机制。源码文件、VS Code 工程选择和其他数据库不受影响。

```bash
tsx scripts/reset-code-intelligence-index.ts --database forexplore_code_intelligence
tsx scripts/reset-code-intelligence-index.ts --database forexplore_code_intelligence --apply
```

第一条仅预览各表行数，第二条删除该数据库的仓库登记、所有分析版本、结构事实、模块产物、检索向量和缓存，再创建空表及同步向量索引。数据库名不变，连接和模型配置复用扩展的 `CODE_INTELLIGENCE_*` 环境变量；更换模型时在同一环境中执行重置与启动。

执行时停止发起分析任务，完成后重新加载扩展，再分析当前显式选择的目标和历史仓库。脚本只接受本机数据库，拒绝系统数据库及包含未知业务表的数据库；不会自动运行或自动重试删除操作。

本次检查发现旧目标登记为 `E:\CS\devsys`，包含 13,202 个文件和 281,560 个符号。当前 VS Code 目标配置已经指向具体的 `commons-fileupload-java-skeleton`，历史仓库为 `account-stream-rs`，无需改动个人设置。新版目标选择只接受显式选择且已打开的目录，不会自动将打开的父目录作为目标。

旧检索表有 281,896 条符号文档、13,218 条源码文档和 9 条摘要；结构符号表还有未完成版本的重复事实。本次重置一并清除。一般性的历史版本保留上限及中断版本自动回收尚未实现；重置不应被当成长期自动清理策略。

真实数据库验证脚本为 `scripts/verify-code-intelligence-reset.ts`，覆盖预览不删除、拒绝未知表、旧异步表重置、重复初始化和新增向量立即召回。

## 本次执行结果

已对 `forexplore_code_intelligence` 执行重置，移除了 295,123 条检索文档、844,917 条符号、389,643 条依赖、39,620 条文件记录、826 条项目记录、2,988 条诊断、7 份模块产物、6 个分析版本和 2 个仓库登记。

重置后仓库、版本、符号和检索文档均为 0，`SHOW CREATE TABLE` 确认向量索引为 `SYNC_MODE=IMMEDIATE`。真实 CodeIntelligenceHost 的空库刷新返回 `ready`，项目模块视图正常构建为空视图，没有旧索引报错。隔离重置验证、20 项目标选择和 Host 测试、服务类型检查通过；扩展宿主 bundle 已重新构建。

重新加载 VS Code 扩展后，按当前配置重新分析目标和历史工程。尚未重新生成模块分析产物；此次完成的是旧数据清除、空索引初始化和刷新路径恢复。
