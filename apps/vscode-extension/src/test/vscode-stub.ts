/**
 * Runtime placeholder used only by Vitest. Individual host tests replace this
 * module with a scoped mock; the extension bundle still externalizes VS Code.
 */
export const window = {};
export const workspace = {};
export const Uri = {};
export const ProgressLocation = { Notification: 15 };
export const ViewColumn = { Beside: 2 };
